# What the test suite itself needs, on top of each service's own
# requirements.txt (the tests import the services, so those must be
# installed too -- see run-tests.sh and .github/workflows/tests.yml).
pytest==9.1.1
pytest-asyncio==1.4.0

# Used by the browser tier only; `pytest -m e2e` skips itself with a reason
# if it is missing, so the rest of the suite runs without it.
playwright==1.63.0
